```mermaid
flowchart TD
    A[Empfange HighState-Nachricht vom Topic /state]
        --> B[Extrahiere Position (x, y) und Ausrichtung (Yaw)]
        --> C[Bestimme Pose des Roboters im odom-Koordinatensystem]
        --> D{Erstelle Odometry-Message\nmit Geschwindigkeit}
        D --> E[Publiziere Odometry auf /odom]
        D --> F[Erstelle TF-Transformation odom → base]
        F --> G[Publiziere TF-Transformation]

```